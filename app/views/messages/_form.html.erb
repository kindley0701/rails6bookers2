<%= form_with model: message, url: user_messages_path, local: false do |f| %>
  <%= f.text_area :body %>
  <%= f.hidden_field :room_id, :value => room.id %>
  <%= f.hidden_field :user_id, :value => current_user.id %>
  <br>
  <%= f.submit 'Send' %>
<% end %>